package dev.azure.desktop.navigation

enum class AppScreen {
    Login,
    PrList,
    PrDetail,
    CodeReview,
    ReleaseList,
    ReleaseDetail,
    /** Stitch canvas design-system asset (tokens + spec); UI-only reference. */
    DesignSystem,
}
